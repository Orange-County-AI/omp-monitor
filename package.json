{
	"name": "omp-monitor",
	"version": "0.1.0",
	"private": true,
	"description": "A generic long-poll / monitor tool for omp: monitor a file or a command and wake the session when it has something to say",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Orange-County-AI/omp-monitor.git"
	},
	"type": "module",
	"omp": {
		"extensions": ["./src/index.ts"]
	},
	"scripts": {
		"smoke": "bun smoke.ts",
		"check": "tsc -p tsconfig.json"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"typescript": "^5.9.3"
	}
}
